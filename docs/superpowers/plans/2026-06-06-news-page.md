# NewsPage 데이터 연동 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** NewsPage에 보유 종목별 최신 뉴스 섹션을 구현한다 — USD는 Finnhub, KRW는 Naver Finance 스크래핑, 종목별 독립 로딩.

**Architecture:** 각 종목 섹션이 `useStockNews(ticker, currency)` 훅을 독립 실행해 자체 loading/error/articles 상태를 관리한다. USD는 `fetchCompanyNews`(Finnhub API 직접 호출), KRW는 `fetchNaverNews`(Cloudflare Function 프록시 → Naver HTML 파싱)를 호출한다. 모두 모듈 레벨 1시간 캐시로 rate limit을 방어한다.

**Tech Stack:** React 18, Vite, Cloudflare Functions, Finnhub API, Naver Finance (비공식), HTMLRewriter, Vitest

---

## 파일 구조

| 파일 | 작업 | 역할 |
|---|---|---|
| `src/utils/finnhub.js` | 수정 | `formatPublishedAt`, `fetchCompanyNews`, `_clearNewsCache` 추가 |
| `functions/api/naver-news.js` | 신규 | CF Function — Naver HTML fetch + HTMLRewriter 파싱 |
| `src/utils/naverNews.js` | 신규 | `fetchNaverNews` — CF 프록시 호출, 1시간 캐시 |
| `src/hooks/useStockNews.js` | 신규 | 종목당 뉴스 훅 (USD/KRW 분기) |
| `src/index.css` | 수정 | 뉴스 CSS 추가 |
| `src/pages/NewsPage.jsx` | 전면 교체 | 종목별 섹션 렌더링 |
| `src/App.jsx` | 수정 | `portfolio` prop을 NewsPage에 전달 |
| `src/__tests__/finnhub.test.js` | 수정 | `formatPublishedAt`, `fetchCompanyNews` 테스트 추가 |
| `src/__tests__/naverNews.test.js` | 신규 | `fetchNaverNews` 테스트 |

---

## Task 1: `fetchCompanyNews` + `formatPublishedAt` — finnhub.js

**Files:**
- Modify: `src/utils/finnhub.js`
- Modify: `src/__tests__/finnhub.test.js`

- [ ] **Step 1: 테스트 작성**

`src/__tests__/finnhub.test.js` 파일에 아래를 추가한다. 기존 import 줄을 교체한다:

```js
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import {
  fetchQuote, fetchEarnings, fetchDividends, _clearDivCache,
  fetchCompanyNews, formatPublishedAt, _clearNewsCache,
} from '../utils/finnhub.js'

beforeEach(() => { _clearDivCache(); _clearNewsCache() })
afterEach(() => vi.restoreAllMocks())
```

파일 끝에 아래 테스트를 추가한다:

```js
describe('formatPublishedAt', () => {
  it('returns "N시간 전" for timestamps within 24 hours', () => {
    const twoHoursAgo = Math.floor((Date.now() - 2 * 60 * 60 * 1000) / 1000)
    expect(formatPublishedAt(twoHoursAgo)).toBe('2시간 전')
  })

  it('returns "YYYY-MM-DD" for timestamps older than 24 hours', () => {
    const ts = Math.floor(new Date('2026-06-01T10:00:00Z').getTime() / 1000)
    expect(formatPublishedAt(ts)).toBe('2026-06-01')
  })
})

describe('fetchCompanyNews', () => {
  it('maps Finnhub response to article shape and slices to 10', async () => {
    const fakeItems = Array.from({ length: 12 }, (_, i) => ({
      headline: `Title ${i}`,
      summary: `Summary ${i}`,
      source: 'Reuters',
      url: `https://example.com/${i}`,
      datetime: Math.floor((Date.now() - i * 60 * 60 * 1000) / 1000),
    }))
    vi.spyOn(global, 'fetch').mockResolvedValueOnce({
      json: () => Promise.resolve(fakeItems),
    })
    const result = await fetchCompanyNews('AAPL', '2026-05-07', '2026-06-06', 'test-key')
    expect(result).toHaveLength(10)
    expect(result[0]).toMatchObject({ title: 'Title 0', source: 'Reuters', url: 'https://example.com/0' })
    expect(typeof result[0].publishedAt).toBe('string')
  })

  it('returns empty array when response is not an array', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValueOnce({
      json: () => Promise.resolve({ error: 'not found' }),
    })
    expect(await fetchCompanyNews('AAPL', '2026-05-07', '2026-06-06', 'test-key')).toEqual([])
  })

  it('returns empty array when apiKey is empty string', async () => {
    expect(await fetchCompanyNews('AAPL', '2026-05-07', '2026-06-06', '')).toEqual([])
  })

  it('returns empty array when fetch throws', async () => {
    vi.spyOn(global, 'fetch').mockRejectedValueOnce(new Error('network'))
    expect(await fetchCompanyNews('AAPL', '2026-05-07', '2026-06-06', 'test-key')).toEqual([])
  })
})
```

- [ ] **Step 2: 테스트 실행 — FAIL 확인**

```bash
npm test -- --run
```

Expected: `fetchCompanyNews`, `formatPublishedAt`, `_clearNewsCache` not defined 오류.

- [ ] **Step 3: `finnhub.js`에 구현 추가**

`src/utils/finnhub.js` 파일 끝(마지막 줄 이후)에 추가한다:

```js
export function formatPublishedAt(unixTs) {
  const diffHours = Math.floor((Date.now() - unixTs * 1000) / (1000 * 60 * 60))
  if (diffHours < 24) return `${diffHours}시간 전`
  const d = new Date(unixTs * 1000)
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`
}

const _newsCache = {}
const NEWS_CACHE_TTL = 60 * 60 * 1000
export function _clearNewsCache() { Object.keys(_newsCache).forEach(k => delete _newsCache[k]) }

export async function fetchCompanyNews(ticker, from, to, apiKey = import.meta.env.VITE_FINNHUB_KEY ?? '') {
  if (!apiKey) return []
  const key = `${ticker}:${from}:${to}`
  if (_newsCache[key] && Date.now() - _newsCache[key].time < NEWS_CACHE_TTL) return _newsCache[key].data
  try {
    const res = await fetch(
      `https://finnhub.io/api/v1/company-news?symbol=${encodeURIComponent(ticker)}&from=${from}&to=${to}&token=${apiKey}`
    )
    const data = await res.json()
    const result = Array.isArray(data)
      ? data.slice(0, 10).map(item => ({
          title: item.headline,
          summary: item.summary || null,
          source: item.source,
          url: item.url,
          publishedAt: formatPublishedAt(item.datetime),
        }))
      : []
    _newsCache[key] = { data: result, time: Date.now() }
    return result
  } catch {
    return _newsCache[key]?.data ?? []
  }
}
```

- [ ] **Step 4: 테스트 실행 — PASS 확인**

```bash
npm test -- --run
```

Expected: 모든 테스트 통과 (기존 113개 + 신규 6개 = 119개).

- [ ] **Step 5: 커밋**

```bash
git add src/utils/finnhub.js src/__tests__/finnhub.test.js
git commit -m "feat: add fetchCompanyNews and formatPublishedAt to finnhub utils"
```

---

## Task 2: Cloudflare Function — `functions/api/naver-news.js`

**Files:**
- Create: `functions/api/naver-news.js`

Cloudflare Function은 Vitest로 직접 테스트하기 어렵다 (HTMLRewriter가 Node에 없음). 브라우저 통합 테스트로 검증한다.

- [ ] **Step 1: 파일 생성**

`functions/api/naver-news.js` 를 아래 내용으로 생성한다:

```js
export async function onRequestGet(context) {
  const url = new URL(context.request.url)
  const code = url.searchParams.get('code') ?? ''
  const headers = {
    'Content-Type': 'application/json',
    'Access-Control-Allow-Origin': '*',
  }

  if (!code.trim()) {
    return new Response(JSON.stringify({ error: 'code required' }), { headers })
  }

  try {
    const naverUrl = `https://finance.naver.com/item/news_news.nhn?code=${encodeURIComponent(code)}&page=1`
    const naverRes = await fetch(naverUrl, {
      headers: { 'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36' },
    })

    const articles = []
    let current = null

    const transformed = new HTMLRewriter()
      .on('table.type5 tbody tr', {
        element(el) {
          current = { title: '', source: '', publishedAt: '', url: '' }
          el.onEndTag(() => {
            if (current?.title?.trim() && current?.url) {
              articles.push({
                title: current.title.trim(),
                summary: null,
                source: current.source.trim(),
                url: current.url,
                publishedAt: current.publishedAt.trim(),
              })
            }
            current = null
          })
        },
      })
      .on('table.type5 tbody tr td.title a', {
        element(el) {
          const href = el.getAttribute('href') ?? ''
          if (current) current.url = href.startsWith('http') ? href : `https://finance.naver.com${href}`
        },
        text(chunk) {
          if (current) current.title += chunk.text
        },
      })
      .on('table.type5 tbody tr td.info', {
        text(chunk) {
          if (current) current.source += chunk.text
        },
      })
      .on('table.type5 tbody tr td.date', {
        text(chunk) {
          if (current) current.publishedAt += chunk.text
        },
      })
      .transform(naverRes)

    await transformed.arrayBuffer()

    const result = articles.filter(a => a.title).slice(0, 10)

    if (result.length === 0) {
      return new Response(JSON.stringify({ error: 'no articles parsed' }), { headers })
    }

    return new Response(JSON.stringify(result), { headers })
  } catch (e) {
    return new Response(JSON.stringify({ error: e.message }), { headers })
  }
}
```

- [ ] **Step 2: 커밋**

```bash
git add functions/api/naver-news.js
git commit -m "feat: add Naver Finance news scraping Cloudflare Function"
```

---

## Task 3: `src/utils/naverNews.js` + 테스트

**Files:**
- Create: `src/utils/naverNews.js`
- Create: `src/__tests__/naverNews.test.js`

- [ ] **Step 1: 테스트 작성**

`src/__tests__/naverNews.test.js` 를 아래 내용으로 생성한다:

```js
// @vitest-environment node
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { fetchNaverNews, _clearNaverCache } from '../utils/naverNews.js'

beforeEach(() => _clearNaverCache())
afterEach(() => vi.restoreAllMocks())

describe('fetchNaverNews', () => {
  it('returns article array when proxy responds successfully', async () => {
    const fakeArticles = [
      { title: '삼성전자 실적 발표', summary: null, source: '한국경제', url: 'https://finance.naver.com/...', publishedAt: '2026.06.06 10:00' },
    ]
    vi.spyOn(global, 'fetch').mockResolvedValueOnce({
      json: () => Promise.resolve(fakeArticles),
    })
    const result = await fetchNaverNews('005930')
    expect(result).toEqual(fakeArticles)
  })

  it('returns null when proxy returns error object', async () => {
    vi.spyOn(global, 'fetch').mockResolvedValueOnce({
      json: () => Promise.resolve({ error: 'no articles parsed' }),
    })
    expect(await fetchNaverNews('005930')).toBeNull()
  })

  it('returns null when fetch throws', async () => {
    vi.spyOn(global, 'fetch').mockRejectedValueOnce(new Error('network'))
    expect(await fetchNaverNews('005930')).toBeNull()
  })
})
```

- [ ] **Step 2: 테스트 실행 — FAIL 확인**

```bash
npm test -- --run
```

Expected: `fetchNaverNews`, `_clearNaverCache` not defined 오류.

- [ ] **Step 3: `naverNews.js` 구현**

`src/utils/naverNews.js` 를 아래 내용으로 생성한다:

```js
const _naverCache = {}
const NAVER_CACHE_TTL = 60 * 60 * 1000
export function _clearNaverCache() { Object.keys(_naverCache).forEach(k => delete _naverCache[k]) }

export async function fetchNaverNews(code) {
  if (_naverCache[code] && Date.now() - _naverCache[code].time < NAVER_CACHE_TTL) {
    return _naverCache[code].data
  }
  try {
    const res = await fetch(`/api/naver-news?code=${encodeURIComponent(code)}`)
    const data = await res.json()
    if (!Array.isArray(data)) return null
    _naverCache[code] = { data, time: Date.now() }
    return data
  } catch {
    return null
  }
}
```

- [ ] **Step 4: 테스트 실행 — PASS 확인**

```bash
npm test -- --run
```

Expected: 모든 테스트 통과 (119개 + 신규 3개 = 122개).

- [ ] **Step 5: 커밋**

```bash
git add src/utils/naverNews.js src/__tests__/naverNews.test.js
git commit -m "feat: add fetchNaverNews utility with 1hr cache"
```

---

## Task 4: `src/hooks/useStockNews.js`

**Files:**
- Create: `src/hooks/useStockNews.js`

이 훅은 순수 함수가 없는 UI 훅이므로 단위 테스트 없이 브라우저에서 통합 검증한다.

- [ ] **Step 1: 파일 생성**

`src/hooks/useStockNews.js` 를 아래 내용으로 생성한다:

```js
import { useState, useEffect } from 'react'
import { fetchCompanyNews } from '../utils/finnhub.js'
import { fetchNaverNews } from '../utils/naverNews.js'

export function useStockNews(ticker, currency) {
  const [articles, setArticles] = useState([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState(null)

  useEffect(() => {
    let cancelled = false
    setLoading(true)
    setError(null)
    setArticles([])

    ;(async () => {
      try {
        let result
        if (currency === 'KRW') {
          result = await fetchNaverNews(ticker)
          if (result === null) throw new Error('fetch failed')
        } else {
          const to = new Date().toISOString().slice(0, 10)
          const from = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString().slice(0, 10)
          result = await fetchCompanyNews(ticker, from, to)
        }
        if (!cancelled) setArticles(result)
      } catch {
        if (!cancelled) setError('뉴스를 불러올 수 없습니다')
      } finally {
        if (!cancelled) setLoading(false)
      }
    })()

    return () => { cancelled = true }
  }, [ticker, currency])

  return { articles, loading, error }
}
```

- [ ] **Step 2: 커밋**

```bash
git add src/hooks/useStockNews.js
git commit -m "feat: add useStockNews hook with USD/KRW branching"
```

---

## Task 5: CSS 추가

**Files:**
- Modify: `src/index.css`

- [ ] **Step 1: CSS 추가**

`src/index.css` 파일 끝(`.type-btn.active.dividend` 블록 다음)에 아래를 추가한다:

```css
/* ── NewsPage ── */

.news-heading {
  font-family: 'Spline Sans Mono', monospace;
  font-size: 11px;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--ink-dim);
  margin: 0 0 24px;
}

.news-section {
  margin-bottom: 40px;
}

.news-section-header {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 12px;
  padding-bottom: 8px;
  border-bottom: 1px solid var(--line);
}

.news-section-ticker {
  font-family: 'Spline Sans Mono', monospace;
  font-size: 12px;
  font-weight: 700;
  color: var(--ink);
}

.news-section-name {
  font-size: 12px;
  color: var(--ink-dim);
}

.news-currency-badge {
  font-family: 'Spline Sans Mono', monospace;
  font-size: 9px;
  letter-spacing: .8px;
  padding: 1px 5px;
  border-radius: 3px;
  margin-left: auto;
}
.news-currency-badge.usd {
  background: rgba(63, 191, 143, 0.12);
  color: #3fbf8f;
}
.news-currency-badge.krw {
  background: rgba(100, 149, 237, 0.12);
  color: #6495ed;
}

.news-card {
  padding: 10px 0;
  border-bottom: 1px solid var(--line);
}
.news-card:last-child { border-bottom: none; }

.news-card-title {
  font-size: 13px;
  color: var(--ink);
  text-decoration: none;
  line-height: 1.4;
  display: block;
  margin-bottom: 4px;
}
.news-card-title:hover { color: var(--accent); }

.news-card-summary {
  font-size: 11px;
  color: var(--ink-dim);
  line-height: 1.5;
  margin-bottom: 5px;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.news-card-meta {
  font-family: 'Spline Sans Mono', monospace;
  font-size: 10px;
  color: var(--ink-faint);
}

.news-empty {
  font-size: 12px;
  color: var(--ink-faint);
  padding: 16px 0;
}

.news-error {
  font-size: 12px;
  color: var(--ink-faint);
  padding: 16px 0;
}
```

- [ ] **Step 2: 커밋**

```bash
git add src/index.css
git commit -m "style: add NewsPage CSS classes"
```

---

## Task 6: `NewsPage.jsx` 구현 + `App.jsx` 수정

**Files:**
- Modify: `src/pages/NewsPage.jsx`
- Modify: `src/App.jsx`

- [ ] **Step 1: `App.jsx` — portfolio prop 전달**

`src/App.jsx` 의 아래 줄을:

```jsx
<Route path="/news" element={<NewsPage />} />
```

이렇게 교체한다:

```jsx
<Route path="/news" element={<NewsPage portfolio={portfolio} />} />
```

- [ ] **Step 2: `NewsPage.jsx` 전면 교체**

`src/pages/NewsPage.jsx` 전체를 아래로 교체한다:

```jsx
import { useStockNews } from '../hooks/useStockNews.js'

function StockNewsSection({ holding }) {
  const currency = holding.currency ?? 'USD'
  const { articles, loading, error } = useStockNews(holding.t, currency)

  return (
    <div className="news-section">
      <div className="news-section-header">
        <span className="news-section-ticker">{holding.t}</span>
        {holding.nm && holding.nm !== holding.t && (
          <span className="news-section-name">{holding.nm}</span>
        )}
        <span className={`news-currency-badge ${currency.toLowerCase()}`}>
          {currency}
        </span>
      </div>

      {loading && <p className="news-empty">조회 중…</p>}
      {error && <p className="news-error">⚠ {error}</p>}

      {!loading && !error && articles.length === 0 && (
        <p className="news-empty">최근 뉴스가 없습니다.</p>
      )}

      {!loading && !error && articles.length > 0 && (
        <div>
          {articles.map((article, i) => (
            <div key={i} className="news-card">
              <a
                className="news-card-title"
                href={article.url}
                target="_blank"
                rel="noopener noreferrer"
              >
                {article.title}
              </a>
              {article.summary && (
                <p className="news-card-summary">{article.summary}</p>
              )}
              <span className="news-card-meta">
                {article.source} · {article.publishedAt}
              </span>
            </div>
          ))}
        </div>
      )}
    </div>
  )
}

export default function NewsPage({ portfolio }) {
  const holdings = portfolio?.holdings ?? []

  if (holdings.length === 0) {
    return (
      <div className="holdings">
        <h2 className="news-heading">뉴스</h2>
        <p className="news-empty">보유 종목을 추가하면 관련 뉴스가 표시됩니다.</p>
      </div>
    )
  }

  return (
    <div className="holdings">
      <h2 className="news-heading">뉴스</h2>
      {holdings.map(h => (
        <StockNewsSection key={h.t} holding={h} />
      ))}
    </div>
  )
}
```

- [ ] **Step 3: 테스트 실행**

```bash
npm test -- --run
```

Expected: 122개 전부 PASS.

- [ ] **Step 4: 커밋**

```bash
git add src/pages/NewsPage.jsx src/App.jsx
git commit -m "feat: implement NewsPage with per-stock news sections"
```

---

## Task 7: 앱 실행 확인 및 push

**Files:** 없음 (검증만)

- [ ] **Step 1: dev 서버 실행**

```bash
npm run dev
```

브라우저에서 `http://localhost:5173` → 사이드바 → 뉴스 메뉴.

- [ ] **Step 2: USD 종목 뉴스 확인**
  1. AAPL 등 USD 보유 종목의 섹션이 보이는지 확인
  2. 로딩 스피너 → 기사 목록으로 전환되는지 확인
  3. 기사 제목 클릭 → 새 탭에서 열리는지 확인
  4. 출처·시간이 `Reuters · 3시간 전` 형식으로 표시되는지 확인

- [ ] **Step 3: KRW 종목 뉴스 확인**
  1. KRW 보유 종목 섹션이 보이는지 확인
  2. 로컬 dev에서는 `/api/naver-news` 라우팅이 없어 에러 표시됨 — 정상
  3. "뉴스를 불러올 수 없습니다" 폴백 문구 확인

- [ ] **Step 4: 빌드 확인 후 push**

```bash
npm run build
git push
```

Cloudflare 배포 후 KRW 종목 뉴스도 실제 동작 확인.
