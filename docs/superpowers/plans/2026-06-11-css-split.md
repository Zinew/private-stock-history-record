# index.css 분리 리팩토링 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 1,354줄 단일 `src/index.css`를 주제별 10개 파일(`src/styles/`)로 분리한다. 클래스명·스타일 동작 변경 0.

**Architecture:** Node 스크립트가 줄 범위 매핑(SEGMENTS)에 따라 원본을 슬라이스해 `src/styles/*.css`를 생성하고, `src/index.css`는 `@import` 목록만 남긴다. 스크립트가 전체 줄 커버리지(빠짐/중복 없음)·파일별 중괄호 균형·교차 파일 중복 셀렉터를 기계적으로 검증한다.

**Tech Stack:** Node.js (의존성 없음, 일회성 스크립트), Vite (빌드 시 @import 인라인 병합), Vitest (기존 203개 테스트)

**Spec:** `docs/superpowers/specs/2026-06-11-css-split-design.md`

---

### Task 1: 분리 스크립트 작성 + 드라이런 검증

**Files:**
- Create: `scripts/split-css.mjs` (일회성 — 최종 커밋에 포함하지 않음)
- Read: `src/index.css` (수정하지 않음, 이 Task에서는 읽기만)

- [ ] **Step 1: 스크립트 작성**

`scripts/split-css.mjs`를 아래 내용 그대로 생성한다:

```js
import fs from 'node:fs'

const SRC = 'src/index.css'
const DRY = process.argv.includes('--dry')
const text = fs.readFileSync(SRC, 'utf8')
const lines = text.split('\n')

// [출력파일, 시작줄, 끝줄] — 1-based, 양끝 포함. src/index.css의 섹션 주석 기준.
const SEGMENTS = [
  ['base.css', 1, 30],          // 폰트 @import, :root, 리셋, body, .wrap
  ['layout.css', 31, 70],       // header, .brand, .summary
  ['base.css', 71, 94],         // .pos/.neg, .grid(+840px), .card
  ['charts.css', 95, 96],       // .chart-box
  ['holdings.css', 97, 214],    // .holdings, table, .tick, .edit/.del, .addbar, .field, .btn
  ['charts.css', 215, 222],     // .snapbar
  ['base.css', 223, 232],       // footer
  ['layout.css', 233, 266],     // currency-toggle, rate-bar
  ['modal.css', 267, 353],      // .modal-*, search-dropdown
  ['layout.css', 354, 453],     // 사이드바, .menu-btn
  ['pages.css', 454, 465],      // .placeholder-page
  ['holdings.css', 466, 537],   // holdings-header, refresh, price-error, market-badge, ticker-error
  ['modal.css', 538, 544],      // .modal-title
  ['holdings.css', 545, 550],   // .auto-label
  ['layout.css', 551, 559],     // .header-right
  ['calendar.css', 560, 728],   // CalendarPage 전체
  ['news.css', 729, 826],       // NewsPage 전체
  ['layout.css', 827, 853],     // 언어 토글
  ['charts.css', 854, 921],     // snapshot delete popup + undo toast
  ['holdings.css', 922, 933],   // Transaction History, sell mode
  ['charts.css', 934, 936],     // BackupBar
  ['pages.css', 937, 1019],     // Static pages + 404
  ['layout.css', 1020, 1038],   // 사이드바 sub-nav
  ['holdings.css', 1039, 1059], // empty state, retry 버튼
  ['mobile.css', 1060, 1232],   // 모바일 카드뷰 + 640px 미디어쿼리
  ['holdings.css', 1233, 1241], // .cash-row
  ['rebalancing.css', 1242, 1330], // 리밸런싱 카드 + 모바일 카드
  ['modal.css', 1331, 1348],    // EditModal 목표비중 행
  ['mobile.css', 1349, 1354],   // 2x2 stats 미디어쿼리
]

// import 순서 — mobile.css가 반드시 마지막 (모바일 오버라이드가 데스크톱 규칙 뒤에 와야 함)
const ORDER = ['base.css', 'layout.css', 'holdings.css', 'modal.css', 'charts.css',
  'rebalancing.css', 'calendar.css', 'news.css', 'pages.css', 'mobile.css']

let failed = false
function fail(msg) { console.error('FAIL: ' + msg); failed = true }

// ── 검증 1: 전체 줄 커버리지 (빠짐/중복 없음) ──
const covered = new Array(lines.length + 1).fill(0)
for (const [, s, e] of SEGMENTS) for (let i = s; i <= e; i++) covered[i]++
for (let i = 1; i <= lines.length; i++) {
  if (covered[i] !== 1) fail(`line ${i} covered ${covered[i]} times: ${lines[i - 1]}`)
}

// ── 출력 조립 (세그먼트는 원본 등장 순서 유지) ──
const out = {}
for (const [file, s, e] of SEGMENTS) {
  (out[file] ??= []).push(lines.slice(s - 1, e).join('\n'))
}

// ── 검증 2: 파일별 중괄호 균형 (규칙 중간 절단 감지) ──
for (const file of ORDER) {
  if (!out[file]) { fail(`no segments for ${file}`); continue }
  let depth = 0
  for (const ch of out[file].join('\n')) {
    if (ch === '{') depth++
    if (ch === '}') depth--
    if (depth < 0) break
  }
  if (depth !== 0) fail(`${file}: unbalanced braces (depth ${depth}) — 세그먼트 경계가 규칙 중간을 자름`)
}

// ── 검증 3: 교차 파일 중복 셀렉터 리포트 (최상위 레벨만) ──
const selToFiles = new Map()
for (const file of ORDER) {
  if (!out[file]) continue
  let depth = 0
  for (const raw of out[file].join('\n').split('\n')) {
    const line = raw.trim()
    if (depth === 0 && /^[.#a-zA-Z*:\[][^{]*\{/.test(line) && !line.startsWith('@')) {
      const sel = line.slice(0, line.indexOf('{')).trim()
      if (!selToFiles.has(sel)) selToFiles.set(sel, new Set())
      selToFiles.get(sel).add(file)
    }
    for (const ch of line) { if (ch === '{') depth++; if (ch === '}') depth-- }
  }
}
for (const [sel, files] of selToFiles) {
  if (files.size > 1) fail(`selector "${sel}" in multiple files: ${[...files].join(', ')}`)
}

if (failed) { console.error('\n검증 실패 — 파일을 쓰지 않음'); process.exit(1) }
console.log(`OK: ${lines.length} lines → ${ORDER.length} files, braces balanced, no cross-file duplicate selectors`)

if (DRY) { console.log('(dry run — 파일 미생성)'); process.exit(0) }

// ── 파일 생성 ──
fs.mkdirSync('src/styles', { recursive: true })
for (const file of ORDER) {
  fs.writeFileSync(`src/styles/${file}`, out[file].join('\n\n') + '\n')
}
fs.writeFileSync(SRC, ORDER.map(f => `@import './styles/${f}';`).join('\n') + '\n')
console.log('생성 완료: src/styles/ 10개 파일 + src/index.css 교체')
```

- [ ] **Step 2: 드라이런 실행**

Run: `node scripts/split-css.mjs --dry`
Expected: `OK: 1354 lines → 10 files, braces balanced, no cross-file duplicate selectors` 후 `(dry run — 파일 미생성)`

- [ ] **Step 3: 검증 실패 시 경계 보정**

실패 유형별 대응 (성공했으면 건너뜀):

- `line N covered 0/2 times` → SEGMENTS의 인접 범위가 N을 빠뜨리거나 겹침. 해당 줄 앞뒤 범위를 ±1 조정.
- `unbalanced braces` → 경계가 CSS 규칙 중간을 자름. `Read`로 `src/index.css`의 해당 경계 부근(±5줄)을 읽고 규칙이 닫히는 `}` 직후로 경계 이동.
- `selector "X" in multiple files` → 동일 셀렉터가 두 그룹에 존재. 두 세그먼트를 같은 파일로 모으되, **원본에서 뒤에 나온 선언이 파일 내에서도 뒤에 오도록** 유지 (SEGMENTS 등장 순서가 곧 파일 내 순서).

보정 후 Step 2를 재실행해 OK가 나올 때까지 반복.

- [ ] **Step 4: 커밋 (스크립트만)**

```bash
git add scripts/split-css.mjs
git commit -m "chore: CSS 분리 스크립트 (검증 포함, 일회성)"
```

---

### Task 2: 분리 실행 + 테스트·빌드 검증

**Files:**
- Create: `src/styles/base.css`, `layout.css`, `holdings.css`, `modal.css`, `charts.css`, `rebalancing.css`, `calendar.css`, `news.css`, `pages.css`, `mobile.css` (스크립트가 생성)
- Modify: `src/index.css` → `@import` 10줄로 교체 (스크립트가 수행)
- Delete: `scripts/split-css.mjs` (커밋 후 제거)

- [ ] **Step 1: 분리 실행**

Run: `node scripts/split-css.mjs`
Expected: `OK: ...` 후 `생성 완료: src/styles/ 10개 파일 + src/index.css 교체`

- [ ] **Step 2: 결과 확인**

Run: `wc -l src/styles/*.css src/index.css`
Expected: styles/ 10개 파일 합계 ≈ 1,380줄 (세그먼트 사이 빈 줄 추가로 원본보다 약간 큼), `src/index.css`는 10줄.

`src/index.css` 내용이 정확히 아래인지 확인:

```css
@import './styles/base.css';
@import './styles/layout.css';
@import './styles/holdings.css';
@import './styles/modal.css';
@import './styles/charts.css';
@import './styles/rebalancing.css';
@import './styles/calendar.css';
@import './styles/news.css';
@import './styles/pages.css';
@import './styles/mobile.css';
```

`src/styles/base.css` 1행이 폰트 `@import url('https://fonts.googleapis.com/...')`인지 확인 (CSS 명세상 @import는 다른 규칙보다 앞에 와야 함 — base.css가 첫 import이므로 충족).

- [ ] **Step 3: 테스트 실행**

Run: `npm test`
Expected: 기존 203개 테스트 전부 PASS (CSS는 테스트 대상이 아니지만 import 경로 깨짐 등 회귀 감지)

- [ ] **Step 4: 빌드 실행**

Run: `npm run build`
Expected: 빌드 성공, 경고 중 CSS 관련(`@import must precede` 등) 없음. `dist/assets/*.css`가 1개로 병합 생성됨.

- [ ] **Step 5: 빌드 CSS 내용 보존 확인**

Run (PowerShell):
```powershell
$css = Get-ChildItem dist/assets/*.css | Get-Content -Raw
@('.rebal-item-card', '.holding-card-stats', '.calendar-card', '.news-card', '.sidebar', '.modal-overlay', '.cash-row', 'max-width:640px') | ForEach-Object { if ($css -notmatch [regex]::Escape($_)) { "MISSING: $_" } }
```
Expected: 출력 없음 (전부 존재). 누락 출력 시 SEGMENTS 매핑 재점검.

- [ ] **Step 6: 분리 결과 커밋, 스크립트 제거**

```bash
git add src/styles src/index.css
git commit -m "refactor: index.css를 주제별 10개 파일로 분리 (src/styles/)"
git rm scripts/split-css.mjs
git commit -m "chore: 일회성 CSS 분리 스크립트 제거"
```

---

### Task 3: 육안 검증 (사용자 확인)

**Files:** 없음 (검증만)

- [ ] **Step 1: dev 서버 실행**

Run: `npm run dev` (백그라운드)
Expected: 로컬 URL 출력 (보통 http://localhost:5173)

- [ ] **Step 2: 사용자 육안 확인 요청**

사용자에게 분리 전후 화면 동일 여부 확인 요청:
- 대시보드 (차트, 보유종목 테이블, 리밸런싱 가이드)
- 캘린더, 뉴스 페이지
- 브라우저 폭 640px 이하로 줄여 모바일 카드뷰 (보유종목 접힘 카드, 거래이력, 리밸런싱 카드)
- About/Help/404 페이지

- [ ] **Step 3: 이상 발견 시**

특정 화면 깨짐 → 해당 화면의 셀렉터가 어느 파일에 들어갔는지 `grep`으로 확인, 누락이면 git 히스토리의 원본 `src/index.css`(커밋 `ca44b09` 시점)에서 해당 규칙을 찾아 올바른 파일에 추가. 수정 후 `npm test` + 사용자 재확인 후 커밋:

```bash
git add src/styles
git commit -m "fix: CSS 분리 누락 규칙 보강 — <셀렉터명>"
```
